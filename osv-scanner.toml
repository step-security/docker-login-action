[[IgnoredVulns]]
id = "GHSA-g9mf-h72j-4rw9"
reason = "Not exploitable in production: undici's fetch() API is not used by this action; HTTP calls go through @octokit/rest and axios which use their own HTTP clients."

[[IgnoredVulns]]
id = "GHSA-2mjp-6q6p-2qxm"
reason = "Not exploitable: undici is a transitive dep of @actions/github, which is only used for github.context.payload (local data). No HTTP calls go through undici; all network requests use axios."

[[IgnoredVulns]]
id = "GHSA-4992-7rv2-5pvq"
reason = "Not exploitable: undici is a transitive dep of @actions/github, which is only used for github.context.payload (local data). No HTTP calls go through undici; all network requests use axios."

[[IgnoredVulns]]
id = "GHSA-v9p9-hfj2-hcw8"
reason = "Not exploitable: undici is a transitive dep of @actions/github, which is only used for github.context.payload (local data). No HTTP calls go through undici; all network requests use axios."

[[IgnoredVulns]]
id = "GHSA-vrm6-8vpv-qv8q"
reason = "Not exploitable: undici is a transitive dep of @actions/github, which is only used for github.context.payload (local data). No HTTP calls go through undici; all network requests use axios."
